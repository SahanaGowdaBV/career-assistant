package career.assistant.settings; import career.assistant.settings.entity.UserSettings; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface SettingsRepository extends JpaRepository<UserSettings,UUID>{Optional<UserSettings> findByOwnerSubject(String ownerSubject);}
